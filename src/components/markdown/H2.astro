---
import anchorIcon from "../icons/link.svg";

interface Props {
  id?: string;
}

const { id } = Astro.props;
---

<h2 id={id}>
  <a href={`#${id}`}><img src={anchorIcon} /><slot /></a>
</h2>
<style>
  img {
    width: 24px;
    height: 24px;

    position: absolute;
    left: -28px;
    height: 100%;
    padding-right: 4px;
    display: none;
  }

  h2 {
    position: relative;
  }

  a {
    text-decoration: none;
    color: var(--color_text-white);
  }

  a:hover > img {
    display: inline;
  }
</style>
